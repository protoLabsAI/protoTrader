id: backtest
name: Backtest
version: 0.1.0
description: >-
  Vectorized strategy backtesting for protoTrader (Slice 2) — canonical
  strategies (MA cross, RSI mean-reversion, breakout, buy-hold) over fetched
  OHLCV with realistic costs/slippage, an out-of-sample split, a buy-and-hold
  benchmark, and a bootstrap Sharpe CI. Disable with `plugins: { disabled: [backtest] }`.
enabled: true
requires_env: []
