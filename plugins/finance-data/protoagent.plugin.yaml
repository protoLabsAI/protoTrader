id: finance-data
name: Finance data
version: 0.1.0
description: >-
  Market-data tools for protoTrader (Slice 1) — no-auth price/OHLCV/fundamentals
  for US equities & ETFs (yfinance) and crypto (ccxt, public exchanges). Needs the
  optional `requirements-finance.txt` extra; the tools return a clear install hint
  until it's present. Disable with `plugins: { disabled: [finance-data] }`.
enabled: true
requires_env: []
